#include "main.h"

/**
*_abs - computes value of an integer
*Description: value of int
*@i:number to be checked
*Return: Returns 0
*/

int _abs(int i)
{
if (i >= 0)
{
return (i);
}
else
{
return (-i);
}
}
