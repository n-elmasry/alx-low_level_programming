#include "main.h"

/**
*print_rev - prints a string, in reverse
*@s: string
*Description: prints a string, in reverse
* Return: s
*/

void print_rev(char *s)
{
int i = 0;

while (s[i] != '\0')
{
c++;
}

for (i -= 1; c >= 0; c--)
{
_putchar(s[c]);
}
_putchar('\n');

}
