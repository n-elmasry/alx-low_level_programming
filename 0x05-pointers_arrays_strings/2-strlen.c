#include "main.h"

/**
*_strlen - returns the length of a string
*@s: lenght
*Description: returns the length of a string
* Return: s
*/
int _strlen(char *s)
{
int c = 0;

for (; *s != '\0'; *s++)
{
c++;
}
}
